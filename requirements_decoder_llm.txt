# Additional requirements for Decoder-Only LLM support
# Install with: pip install -r requirements_decoder_llm.txt

# Core dependencies (if not already installed)
torch>=2.0.0
transformers>=4.35.0
tokenizers>=0.13.0

# For PEFT (LoRA, Adapter)
peft>=0.6.0

# For datasets (GSM8K, MATH)
datasets>=2.14.0

# For training
accelerate>=0.24.0

# Utilities
numpy>=1.24.0
pandas>=2.0.0
tqdm>=4.65.0

# Optional (for visualization)
matplotlib>=3.7.0
seaborn>=0.12.0

# Optional (for model quantization)
bitsandbytes>=0.41.0
